name: 'jdboss'
version: '1.0.0'
config-version: 2

profile: 'jdboss'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  jdboss:
    staging:
      +schema: public
      +materialized: table
    migrations:
      +schema: public
      +materialized: table
    inject:
      +schema: public
      +materialized: table
